import * as ActionTypes from '../../../action-types';

export const receiveRecognitions = () => ({type: ActionTypes.RECEIVED_RECOGNITIONS});
export const requestRecognitions = () => ({type: ActionTypes.REQUEST_RECOGNITIONS});

